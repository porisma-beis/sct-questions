{
  "name": "supply-chain-transparency-content",
  "version": "0.1.0",
  "private": true,
  "description": "Public content repository for the Supply Chain Transparency Graph",
  "scripts": {
    "build": "node ./scripts/build.js",
    "validate": "node ./scripts/build.js --check"
  },
  "engines": {
    "node": ">=20"
  }
}
